{{!--
blogposts

Post Title

Date created
--}}
{{#each posts as |post| }}

<div class="card">
    <div class="card-header">
        {{post.title}}
    </div>
    <div class="card-body">
        <h5 class="card-title">{{post.user.username}} and </h5>
        {{!-- <a href="#" class="btn btn-primary">Leave a Comment 💬</a> --}}
    </div>
</div>
{{/each}}